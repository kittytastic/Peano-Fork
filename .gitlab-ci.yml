build1:
  stage: build
  script:
    - cd cookbook && latexmk -shell-escape -view=pdf cookbook.tex
  #  - scp cookbook/cookbook.pdf "$CIUSER"@"$CIMACHINE":"cookbook_$CI_COMMIT_SHA.pdf"
  artifacts:
    paths:
      - cookbook/cookbook.pdf
    expire_in: 1 week

build2:
  tags:
    - cosma7
  script:
    - hostname
    - whoami
    - pwd
    - ls
