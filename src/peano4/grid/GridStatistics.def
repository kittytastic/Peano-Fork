
Packed-Type: short int;


class peano4::grid::GridStatistics {
  persistent parallelise double  numberOfRefinedVertices;
  persistent parallelise double  numberOfUnrefinedVertices;
  persistent parallelise double  numberOfErasingVertices;
  persistent parallelise double  numberOfRefiningVertices;
};
