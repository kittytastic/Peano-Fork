Packed-Type: short int;

Constant: Dimensions hint-value 4;


class peano4::grid::GridControlEvent {
  enum RefinementControl {
    Refine,
    Erase
  };
  
  persistent parallelise RefinementControl  refinementControl;
  
  persistent parallelise packed double  offset[Dimensions];
  persistent parallelise packed double  width[Dimensions];
  persistent parallelise packed double  h[Dimensions];
};

