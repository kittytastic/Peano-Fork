Packed-Type: short int;
// have to know whether we are using MPI
#include "config.h"

Constant: Dimensions hint-value 4;


class peano4::grid::GridControlEvent {
  enum RefinementControl {
    Refine,
    Erase
  };
  
  persistent parallelise RefinementControl  refinementControl;
  
  persistent parallelise double  offset[Dimensions];
  persistent parallelise double  width[Dimensions];
  persistent parallelise double  h[Dimensions];
};

