#include "peano4/utils/Globals.h"
// have to know whether we are using MPI
#include "config.h"

Packed-Type: short int;


class peano4::parallel::TreeManagementMessage {
  enum Action {
    RequestNewRemoteTree,
    BookedNewRemoteTree,
    CreateNewRemoteTree,
    RemoveChildTreeFromBooksAsChildBecameEmpty
  };
  persistent        parallelise int     masterSpacetreeId;
  persistent        parallelise int     workerSpacetreeId;
  persistent packed parallelise Action  action;   
};
